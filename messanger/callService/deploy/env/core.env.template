# Генерируется из deploy/env/.env через task env:generate → deploy/compose/core/.env
# Используется docker-compose в deploy/compose/core

KAFKA_INTERNAL_PORT=${CORE_KAFKA_INTERNAL_PORT}
KAFKA_EXTERNAL_PORT=${CORE_KAFKA_EXTERNAL_PORT}
KAFKA_CONTROLLER_PORT=${CORE_KAFKA_CONTROLLER_PORT}
KAFKA_UI_PORT=${CORE_KAFKA_UI_PORT}

REDIS_EXTERNAL_PORT=${CORE_REDIS_EXTERNAL_PORT}

PROMETHEUS_EXTERNAL_PORT=${CORE_PROMETHEUS_EXTERNAL_PORT}
GRAFANA_EXTERNAL_PORT=${CORE_GRAFANA_EXTERNAL_PORT}
GRAFANA_ADMIN_USER=${CORE_GRAFANA_ADMIN_USER}
GRAFANA_ADMIN_PASSWORD=${CORE_GRAFANA_ADMIN_PASSWORD}
