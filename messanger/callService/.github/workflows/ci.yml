# Deprecated location — GitHub Actions only reads workflows from the repository root.
# Active workflow: ../../../../.github/workflows/callservice.yml
#
# Kept so local docs still point somewhere; do not add jobs here.
name: (moved) callService CI

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow path is ignored by GitHub when callService is not the repo root."
          echo "Use .github/workflows/callservice.yml at the repository root."
          exit 0
